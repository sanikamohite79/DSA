
using System;
class Program
{
   static void Main()
    {
         void Greet()
        {
            Console.WriteLine("Hello Roshan...!");
        }
        Greet();
    }
}

//A local function is declared inside another function. It can only be used within that function.

//Function Used
//Greet() → Local function that adds two numbers.
//Main() → Contains and calls the local function.